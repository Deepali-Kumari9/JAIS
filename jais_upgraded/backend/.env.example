# Optional: Add your Anthropic API key for live AI extraction
# Without it, JAIS uses rule-based extraction (works perfectly for demo)
ANTHROPIC_API_KEY=

# Database path (leave as default)
DB_PATH=jais.db

# Server port
PORT=8000
